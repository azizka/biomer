
biome_classify_records <- function(x,
                                   latitude = "latitude"
                                   longitude = "longitude"
                                   biome.scheme = c()){
  
  # reproject occurrence records
  
  # extract biome values
  
  # generate return data.frame
  
  # return values
  
  # argument to limit to a specific biome definition or to batch apply
  
}
