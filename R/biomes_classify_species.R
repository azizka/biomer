biome_classify_species <- function(x,
                                   latitude = "latitude"
                                   longitude = "longitude"
                                   species.name = "species"
                                   occurrence.threshold = 0.05, 
                                   biome.scheme = c()){
  
  # assertions
  
  # classify records
  
  # summarize pers speccies
  
  # apply threshold
  
  # prepare output data.frame
  
  # return value
  
}